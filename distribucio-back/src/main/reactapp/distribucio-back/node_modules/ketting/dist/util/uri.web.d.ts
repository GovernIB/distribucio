import { Link } from '../link';
type UrlParts = {
    host?: string;
};
/**
 * Resolves a relative url using another url.
 *
 * This is the browser-based version.
 */
export declare function resolve(base: string, relative: string): string;
export declare function resolve(link: Link): string;
/**
 * Parses a url in multiple components.
 *
 * This is the browser-based version.
 */
export declare function parse(url: string): UrlParts;
export {};
